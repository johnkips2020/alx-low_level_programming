#include <stdio.h>

/**
*main- prints letters of alphabet
*except q and e follow new line
*Return: 0 terminates program
*/
int main(void)
{
	char ch = 'a';

	while(ch <= 'z')
	{
while (ch <= 'z')
	{
		if (ch != 'e' && ch != 'q')
		{
			putchar(ch);
		}
		ch++;
	}
	putchar('\n');
	return (0);
}
